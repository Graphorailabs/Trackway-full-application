// Schematic editor constants
// Toggle to disable autosave for testing/clean refreshes.
// Set to `true` to prevent the editor from writing autosave data.
export const DISABLE_AUTOSAVE = false;
// Disable wire hover/highlight UI (pencil icon and selected thickening)
export const DISABLE_WIRE_HIGHLIGHT =true;
