// SVG-based footprint layer is deprecated. The project now uses Konva for
// footprint rendering inside the main Stage (`FootprintKonvaLayer`). This file
// remains as a no-op to avoid accidental imports elsewhere.
export default function FootprintLayer() {
  return null;
}
