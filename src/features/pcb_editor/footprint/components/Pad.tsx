// Deprecated SVG Pad renderer. Use Konva-based pad rendering in
// `FootprintKonvaRenderer`/`FootprintKonvaLayer` instead. Keep a no-op
// component here so any stray imports don't break.
export default function Pad() {
  return null;
}
