// SVG-based footprint renderer (deprecated)
// The project has migrated to a Konva-based renderer. Keep a minimal stub
// here so any remaining imports do not break while the transition completes.
export default function Footprint() {
  return null;
}
